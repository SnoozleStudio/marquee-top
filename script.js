Marquee3k.init();

console.log(Marquee3k);

window.addEventListener("resize", () => {
  // Refresh all instances
  Marquee3k.refreshAll();
});

console.log("SnoozleStudio Marquee Top Plugin is Active! 💪");
